/* WebClass configuration - LOCAL MODE
 * Login siswa/admin and website data use the original browser-based system.
 * No PostgreSQL, Supabase, JWT, or external backend is required.
 */
window.WEBCLASS_API_URL = '';
